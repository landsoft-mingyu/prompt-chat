# 웹 프레임워크
fastapi
# ASGI 서버 (standard = websockets, httptools 등 고성능 옵션 포함)
uvicorn[standard]
# 환경변수 → 설정 객체 자동 매핑 (pydantic v2 기반)
pydantic-settings
# SQLAlchemy CUBRID 방언 + pycubrid 드라이버
sqlalchemy-cubrid[pycubrid]
# HTTP 클라이언트 (외부 API 호출용 비동기 클라이언트, 임베딩 서비스 호출 포함)
httpx
# 벡터 DB 클라이언트 (site_chunks 검색/적재)
pymilvus
# Redis 비동기 클라이언트 (세션 저장소)
redis[asyncio]